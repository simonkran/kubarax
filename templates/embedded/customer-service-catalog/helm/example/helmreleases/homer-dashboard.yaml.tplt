{{- if eq (index .services "homerDashboard").status "enabled" }}
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: homer-dashboard
  namespace: flux-system
spec:
  interval: 10m
  releaseName: homer-dashboard
  chart:
    spec:
      chart: ./managed-service-catalog/helm/homer-dashboard
      sourceRef:
        kind: GitRepository
        name: flux-system
      interval: 5m
  targetNamespace: homer
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  dependsOn:
    - name: cert-manager
    - name: traefik
  values: {}
{{- end }}
